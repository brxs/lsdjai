{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capability — Tauri core plus WebMIDI via tauri-plugin-midi (ADR-0005).",
  "windows": ["main"],
  "permissions": ["core:default", "midi:default"]
}
