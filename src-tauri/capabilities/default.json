{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window capability — Tauri core, the native folder picker (dialog open), and window dragging for the frameless header (data-tauri-drag-region). MIDI is native in the shell (ADR-0031) and needs no webview permission.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-open"
  ]
}
